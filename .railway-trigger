DEPLOYMENT_TRIGGER_V78
Timestamp: 2026-06-04T12:00:00+05:30
Status: Force rebuild — email banner naming + admin coverage strip
Changes:
  - "Your view:" renamed to "University:" / "Universities:" (auto-pluralizes
    when multiple) so the label matches operator vocabulary.
  - Admin recipients now get a two-column coverage strip:
      • GREEN box: "Universities with data (N): list..."
      • RED box: "Universities without data (N): list..."
    sourced from fee_scope.getUniversityAvailability(window_id). Excludes
    operational non-campus rows (Central Team, CRM, Student Engagement
    Team) from the "without" list so they don't pollute it.
  - Logo URL is reachable in production (HTTP 200 confirmed); the broken
    icon in local previews was Chrome blocking remote images on file://.
  - Verified end-to-end with 3 local renders (admin, single-uni BOA,
    multi-uni BOA) — all under 17 KB so no Gmail clipping risk.
