DEPLOYMENT_TRIGGER_V65
Timestamp: 2026-06-03T09:55:00+05:30
Status: Force rebuild — fix Svelte 5 {@const} placement errors blocking app build
Changes:
  - {@const} for gauge (r/circ/dash), donut (total/r/circ/fully/
    partial/yet) and chart max values (maxPayable, maxTag) were
    placed inside plain <div> elements; Svelte 5 only allows
    {@const} as the immediate child of control-flow blocks.
  - Hoisted all of them to the top of {#if tab === 'overview'} so
    the computed values are in scope for the whole overview view.
  - Verified locally: vite build now succeeds (4119 + 3776 modules
    transformed, no errors).
