DEPLOYMENT_TRIGGER_V71
Timestamp: 2026-06-03T12:45:00+05:30
Status: Force rebuild — bundle Roboto font + drop empty Operator-tag section
After V70 still rendered with no chart text on Railway (nixpacks fonts
didn't reach the runtime container), switched to embedding Roboto
Regular Latin subset as base64 directly in the JS bundle. Bytes:
- TTF: 124KB
- base64 module: ~165KB
At module load, GlobalFonts.register() makes it the 'AppFont' family.
Every chart uses 'AppFont, DejaVu Sans, Arial, sans-serif' so it
falls through cleanly on every OS. No more dependency on system fonts.

Also: Operator-tag distribution section moved to the BOTTOM of the
Analytics sheet AND skipped entirely when no operator tags are set
yet — user pointed out it was wasting space at the top with just a
"(no tags applied yet)" placeholder.
