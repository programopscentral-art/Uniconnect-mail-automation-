DEPLOYMENT_TRIGGER_V69
Timestamp: 2026-06-03T12:25:00+05:30
Status: Force rebuild — website Overview analytics expansion
Changes:
  - Migration 0099: fee_collection_snapshot daily time-series table
  - Sync flow now writes a daily snapshot (idempotent per window+IST-date)
  - Page server: trend query (last 30 days) + extended success_coaches
    with payable/paid totals
  - Overview UI additions:
      • Collection % trend chart — SVG line + gradient area, last 30
        days, with empty state when <2 points exist
      • Per-success-coach performance table — sorted by coll % desc,
        top 8, with status counts + ₹ totals + colored coll %
      • Needs attention panel — bottom 5 universities by coll %
        (min 50 students), red-tinted card
      • Dropout reasons capped at top 8 + "Other" bucket
      • Tag-case chart now filters out 'Dropout'; label clarifies why
