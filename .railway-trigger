DEPLOYMENT_TRIGGER_V66
Timestamp: 2026-06-03T10:25:00+05:30
Status: Force rebuild — fee-collection sync 1000x faster via batched upserts
Changes:
  - syncBatchSubsheet: collects all student rows in memory, then
    bulk-upserts in chunks of 500 (one INSERT per chunk vs one per
    student). 4,537 students: was ~19 min, now ~1-2 sec.
  - syncDatesSubsheet: one bulk INSERT per batch_period instead of
    per (batch × university). 3 round-trips vs ~54.
  - syncDropoutSubsheet: single lookup query for all dropout →
    batch_period mappings (was N queries), one bulk INSERT for the
    dropout_log (was N), one bulk UPDATE for the Dropout auto-tag
    (was N). 84 dropouts: was ~84s, now ~50ms.
  - In-memory dedup of duplicate zoho_user_id within a sheet to
    avoid Postgres "cannot affect row a second time" on conflict.
