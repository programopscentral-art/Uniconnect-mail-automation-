DEPLOYMENT_TRIGGER_V67
Timestamp: 2026-06-03T10:55:00+05:30
Status: Force rebuild — dropout accuracy + analytics in report
Changes:
  - Dropout sync now reads 'UID' (the UUID matching batch sheets'
    User ID) BEFORE 'NIAT ID' so dropouts link to fee_student_payments
  - Reason candidates now include 'Reason for dropout' (the actual
    primary column) — previously only matched 'Remarks'
  - Overview dropout count sourced from fee_dropout_log directly
    (was tag_case='Dropout' which only counted matched dropouts → 0)
  - New "Top dropout reasons" widget on Overview tab (rose→red bars)
  - New "Analytics" sheet in the Excel workbook with:
      • Top dropout reasons (count + % of total)
      • Tag-case distribution
      • University leaderboard (top 5 by collection %, min 50 students)
      • Needs attention (bottom 5 by collection %)
      • Batch comparison strip
  - Backfilled 69 existing dropout rows: 10 now linked to batches,
    10 students auto-tagged as Dropout
