DEPLOYMENT_TRIGGER_V77
Timestamp: 2026-06-04T11:20:00+05:30
Status: Force rebuild — per-recipient scoped reports (email + XLSX)
Changes:
  - New apps/app/src/lib/server/fee_scope.ts — getUserUniversityScope(),
    getEmailUniversityScope(), scopeLabel(): ADMIN/PROGRAM_OPS → 'all',
    PM/PMA/BOA/CMA → universities of their assigned campuses,
    COS → universities of their cluster's campuses.
  - buildSnapshot() + fireSnapshot() now compute per-recipient scope
    and send each PM/COS a personalised email summing only their
    own universities. Same kind dedup as before.
  - Snapshot email: maroon-header date moved below the title so it no
    longer wraps. Adds an amber "Your view: <universities>" strip
    between the window name and the hero cards when scoped.
  - report.xlsx endpoint reads locals.user scope and appends a SQL
    filter to every fee_student_payments / fee_dropout_log query.
    Summary sheet shows a scope row when present.
