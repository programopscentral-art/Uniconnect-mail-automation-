DEPLOYMENT_TRIGGER_V80
Timestamp: 2026-06-04T12:30:00+05:30
Status: Force rebuild — expand snapshot recipients to 72; exclude CMA + ops placeholders
Changes:
  - resolveSnapshotRecipients() now picks role IN ('PM','BOA') from
    user_campus_assignment (was 'PM' only). That brings the 41 BOA-role
    operators into the snapshot mailing list. Total grows from 31 → 72.
  - Hard-exclude roles even if they leak into the candidate set:
    CMA, CMA_MANAGER, STUDENT, FACULTY, UNIVERSITY_OPERATOR.
  - Exclude assignments whose university is an operational placeholder
    ('Student Engagement Team', 'CRM', 'Central Team') so a stray
    BOA/PM assigned to one of those buckets doesn't pull them in.
  - Worker uses the same endpoint, so no separate worker change needed.
  - Today's manual dedup rows (31) were cleared from the DB so the
    next Send-Snapshot click delivers cleanly to the new 72 list.
