DEPLOYMENT_TRIGGER_V68
Timestamp: 2026-06-03T11:55:00+05:30
Status: Force rebuild — Analytics sheet reordered + native PNG charts
Changes:
  - Analytics sheet reordered: Collection KPIs → Status doughnut →
    Batch table + bar chart → Top universities table + bar chart →
    Needs attention → Operator tag distribution → Top dropout reasons
    table + bar chart (capped at top 10 + "Other")
  - Five PNG charts embedded via @napi-rs/canvas:
      • Payment status doughnut
      • Collection % by batch (vertical bars, color-coded by tier)
      • Top 10 universities (horizontal bars, payable rail + paid fill)
      • Top 10 dropout reasons (horizontal bars)
      • Operator tag distribution (vertical bars, violet→fuchsia)
  - Tag-case chart excludes 'Dropout' so it doesn't double-count
    against the dropout headline; small label clarifies why
  - Dropout reasons capped at top 10 with the rest as "Other"
  - Each PNG rendered directly with @napi-rs/canvas (already in deps,
    no native compilation on Railway)
