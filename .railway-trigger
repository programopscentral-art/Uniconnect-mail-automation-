DEPLOYMENT_TRIGGER_V83
Timestamp: 2026-06-08T14:35:00+05:30
Status: Force rebuild — fix VGU/Takshashila/Yenepoya missing from per-batch table
Changes:
  - apps/app/src/routes/api/fees2/batches/[id]/students/+server.ts:
    bumped LIMIT 5000 → LIMIT 20000.
      Batch 2025 · Semester 3 has 6,000 students; the old limit cut
      the response at 5,000. Because results are ORDER BY u.name,
      fsp.student_name, the cut landed alphabetically inside Sanjay
      Ghodawat (only 334 of 414 loaded), and Takshashila, VGU,
      Yenepoya never loaded at all. The "University-wise within
      this batch" panel aggregates client-side from the loaded
      students array, so those three were silently missing.
      LIMIT 20000 covers ~3x current largest batch with headroom.
  - No other code or data changes in this push.
